# Findings réfutés par le vérificateur adversarial

Ces findings n'ont PAS survécu à la réfutation et sont exclus du registre.

## BUG-044 — E2E create-mock-video instable : timeout 30s alors que le flux produit bien un média (race non maîtrisée) (test-mock-infrastructure)
- **Claim auditeur (réel):** FAILED: 'Test timeout of 30000ms exceeded' en attente du bouton 'Générer un visuel IA' (l.28). MAIS le bloc DIAGNOSTIC du même run montre que le flux a bien produit un média lié (me_uu3topxhsoux2dwp, toast 'Visuel IA généré', PreviewPane visible). Les assets mock MP4/PNG sont servis 200. Le bug est une condition de course UI (le bouton n'est pas présent au moment du click selon le timing de génération des variantes), pas un défaut fonctionnel.
- **Réfutation:** Le test echoue bien (timeout 30s l.28) MAIS la cause racine 'condition de course UI / le bouton n apparait qu apres stabilisation' est FAUSSE, et la preuve 'le meme run a produit un media' est MAL ATTRIBUEE. (1) Mauvaise attribution: le bloc DIAGNOSTIC (Outcome:variants, media me_uu3topxhsoux2dwp kind:'image', toast 'Visuel IA genere', media-bound:true) appartient a generation-smoke.spec.ts:13 ('Generation smoke real backend'), PAS a create-mock-video:8 — voir log [33/39] generation-smoke juste avant le DIAGNOSTIC; ce media est une IMAGE (<video> srcs VIDE). (2) Cause reelle deterministe, pas une race: le spec :8 cree un REEL (kind=video, il assert lui-meme aria-checked='true' sur le toggle video l.22) puis attend le bouton /Generer un visuel IA/i (l.28). Or MediaStudio.tsx:240 affiche 'Generer une VIDEO IA' quand kind==='video' et 'Generer un VISUEL IA' seulement pour l image. Le regex ne matchera JAMAIS => timeout deterministe. (3) Preuve par le test frere :39 (kind=image) qui bascule explicitement en data-cs-kind=image puis attend 'Generer un visuel IA' et PASSE. retries=0 en local. C est un bug d assertion (mauvais libelle), pas une instabilite.
- **Contre-preuve:** MediaStudio.tsx:240 'kind === video ? Generer une video IA : Generer un visuel IA'. Spec :8 l.22 assert le toggle video actif, puis l.28 attend /Generer un visuel IA/i (libelle IMAGE) => impossible a matcher. Test frere :39 (image) avec le meme libelle passe (dans les 37 passed). Log: la section DIAGNOSTIC media-bound suit '[33/39] generation-smoke.spec.ts:13' et montre kind:'image' + '<video> srcs:' vide — donc media produit par generation-smoke, pas par create-mock-video.

